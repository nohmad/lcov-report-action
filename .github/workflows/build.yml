name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

defaults:
  stages:
    - staging
    - v2
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - run: |
        echo ${{ defaults.stages }}
      
