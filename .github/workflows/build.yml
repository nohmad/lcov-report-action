name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      stages:
        - staging
        - v2
  

    steps:
    - run: |
        echo ${{ jobs.build.defaults.stages }}
