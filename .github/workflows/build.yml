name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - run: |
        echo ${{ fromJSON('{"a":"b"}') }}
        echo ${{ contains(fromJSON('["staging","v2"]'), 'staging') }}
